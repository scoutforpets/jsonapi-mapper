language: node_js

node_js:
    - "6.0"

# Libraries install and database migration
install:
    - npm install

script: npm test

# Branches white list
branches:
    - only:
        - master
        - develop
        - testing
